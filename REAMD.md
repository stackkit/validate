```js

const rules = {
  fields: {
    email: {
      validator: () => {},
    },
    password: {
      validator: () => {},
    }
  }
}

const callback = (messages) => {

}

validate({
  rules,
}, callback)
```
